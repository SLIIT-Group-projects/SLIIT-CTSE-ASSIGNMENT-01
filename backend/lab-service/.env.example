NODE_ENV=development
PORT=4004

# Use separate DB per service
MONGODB_URI=mongodb+srv://dbDaham:dbdaham@cluster0.nlbd9xi.mongodb.net/hm_lab

# Shared across all backend services
JWT_SECRET=replace_with_secure_shared_secret
JWT_EXPIRES_IN=7d
INTERNAL_SERVICE_TOKEN=replace_with_secure_internal_token

# Inter-service URLs — Docker: use service names (e.g. auth-service:4001). Local node: http://localhost:4001
BILLING_SERVICE_BASE_URL=http://billing-service:4005
AUTH_SERVICE_BASE_URL=http://localhost:4001
DEFAULT_LAB_AMOUNT=300

# Used to generate absolute report URLs
PUBLIC_SERVICE_BASE_URL=http://localhost:4004
PUBLIC_API_BASE_URL=http://localhost:4004

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=sajithadilumina2002@gmail.com
SMTP_PASS=bckd ejjo noyr swie
MAIL_FROM=sajithadilumina2002@gmail.com

# Port 465 SSL (some hosts): set SMTP_PORT=465 and SMTP_SECURE=true
